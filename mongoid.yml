development:
  sessions:
    default:
      hosts:
        - localhost:27017
      database: prgrphs_development
production:
  sessions:
    default:
      uri: <%= ENV['MONGOHQ_URL'] %>
      options:
        timeout: 15
